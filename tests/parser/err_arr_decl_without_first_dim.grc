fun main () : nothing
   
   $ The following should be illegean because the type of a var declaration
   var r : char[];

   fun reverse (ref s : char[]) : nothing
      var i, l : int;
   {
      l <- strlen(s);
      i <- 0;
      while i < l do {
         r[i] <- s[l-i-1];
         i <- i+1;
      }
      r[i] <- '\0';
   }

{ $ main
   reverse("\n!dlrow olleH");
   writeString(r);
} $ main
